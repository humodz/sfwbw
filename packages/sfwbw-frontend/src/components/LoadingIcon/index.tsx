import { icons } from '@sfwbw/sfwbw-assets';

export function LoadingIcon() {
  return (
    <img
      src={icons.loading}
      alt="Loading"
      style={{
        height: '20px',
        width: '20px',
      }}
    />
  );
}
